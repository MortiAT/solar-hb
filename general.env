PGDATA=/pgdata
POSTGRES_PASSWORD=password
POSTGRES_USER=postgres
POSTGRES_DB=postgres

PGRST_DB_URI="postgres://$POSTGRES_USER:$POSTGRES_PASSWORD@timescale:5432/$POSTGRES_DB"
PGRST_SERVER_HOST="0.0.0.0"
PGRST_OPENAPI_SERVER_PROXY_URI="http://127.0.0.1:3000"
PGRST_DB_ANON_ROLE = "reader"

PYTHONUNBUFFERED=1  # needed so that pythons print output is visible in the logs
#      - FLASK_ENV=production
POSTGREST_URL="http://192.168.1.151:3000"

TZ=Europe/Vienna

